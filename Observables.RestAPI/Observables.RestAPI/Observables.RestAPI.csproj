<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Observables.RestAPI</RootNamespace>
    <AssemblyName>Observables.RestAPI</AssemblyName>
    <Description>Declarative type-safe HTTP client runtime, backend-neutral: R3 and System.Reactive types appear only in generated proxies.</Description>
  </PropertyGroup>


  <ItemGroup>
    <InternalsVisibleTo Include="Observables.RestAPI.Tests" />
  </ItemGroup>

  <ItemGroup Condition="'$(TargetFramework)' == 'netstandard2.0'">
    <PackageReference Include="System.Text.Json" />
    <PackageReference Include="System.Net.Http.Json" />
  </ItemGroup>

</Project>
